No user action is needed beyond the account tag configuration above.
Once installed, the module transparently changes the Concepto values in
the CREDITO/REST_CREDITO files generated by the existing "VAT Simple
Report (ZIP)" export button on the IVA Simple report - the export
workflow itself is unchanged.
